%table.table.table-striped
  %thead
    %tr
      %th= t('activerecord.models.product.one')
      %th= t('activerecord.models.firm.one')
      %th= t 'attributes.date'
      %th= Interest.human_attribute_name :price
      %th
  %tbody{ id: 'sale_interests_body' }
    = render partial: 'sale_interests/sale_interest', as: :interest, collection: @sale_interests