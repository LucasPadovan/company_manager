<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="iconic">&#xe055;</span>
      </a>
      
      <%= link_to t('app_name'), root_path, class: 'brand' %>
      
      <% if user_signed_in? %>
        <div class="nav-collapse">
          <ul class="nav">
            <%= show_menu_link( name: 'Empresas',    controllers: 'firms',             path: firms_path             ) if can?( :read, Firm ) %>
            <%= show_menu_link( name: 'Productos',   controllers: 'products',          path: products_path          ) if can?( :read, Product ) %>
            <!--<%= show_menu_link( name: 'Stock',       controllers: 'products',          path: products_path          ) if can?( :read, Product ) %>-->
            <!--<%= show_menu_link( name: 'Producción',  controllers: 'products',          path: products_path          ) if can?( :read, Product ) %>-->
            <%= show_menu_link( name: 'Pedidos',     controllers: 'orders',            path: orders_path            ) if can?( :read, Order ) %>
            <%#= show_menu_link( name: 'Remitos',     controllers: 'receipts',            path: receipts_path            ) if can?( :read, Receipt ) %>
            <%= show_menu_link( name: 'Facturación', controllers: 'monthly_movements', path: monthly_movements_path ) if can?( :read, MonthlyMovement ) %>
            <%= show_menu_link( name: 'Usuarios',    controllers: 'users',             path: users_path             ) if can?( :read, User ) %>
          </ul>
          <%= render partial: 'shared/nav_search' %>
          <ul class="nav pull-right">
            <li>
              <%= link_to(
                'Perfil', edit_profile_user_path(current_user),
                'title' => t('view.users.edit_profile'),
                'data-show-tooltip' => true, 'data-placement' => 'bottom'
              ) %>
            </li>
            <li>
              <%= link_to(
                '&#x2192;'.html_safe, destroy_user_session_path,
                'title' => t('menu.actions.logout'), 'class' => 'iconic',
                'id' => 'logout', 'data-show-tooltip' => true,
                'data-placement' => 'bottom', 'method' => :delete
              ) %>
            </li>
          </ul>
        </div>
      <% end %>
    </div>
  </div>
</div>
