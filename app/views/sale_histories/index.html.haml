.container-fluid
  %h1 Precios de venta de #{@product.name}
  %table.table.table-bordered.table-striped
    %tr
      %th Fecha
      %th Empresa
      %th Precio venta
      %th

    - @sale_histories.each do |sale_history|
      %tr
        %td= sale_history.date.strftime('%d/%m/%y - %H:%M')
        %td= sale_history.firm.nombre
        %td= sale_history.price
        %td= link_to_destroy [@product, sale_history], method: :delete, data: { confirm: 'Are you sure?' }
.form-actions
  = link_to 'Volver', @product, class: 'btn'
  = link_to 'Nuevo precio', new_product_sale_history_path(@product), class: 'btn btn-primary'
